The following bugs and improvements from the checklist have been successfully fixed, refactored, and verified:

1. Routing Cleanup:
   - Deleted legacy conflicting product pages under `src/app/admin/products/`.
   - Added a permanent redirect to `/admin/dashboard/products` in the legacy layout.

2. Pricing Logic Consolidation:
   - Centralized product offers, base prices, and discount calculations in `getProductPricing` (src/lib/offers/pricing.ts).
   - Refactored all queries and APIs (cart, orders) to use this centralized helper, ensuring consistent discount representation.

3. Shop Products & Filters:
   - Rewrote the products list page as a Server Component for proper SEO.
   - Integrated the responsive mobile filters within the mobile sheet drawer.

4. Input Validation:
   - Added Zod schema validation to the PUT product API endpoint to validate the request body.

5. Blog & Support UI:
   - Created a client-side `DeletePostButton` to prevent hydration mismatches on the admin blog list page.
   - Optimized chat polling intervals to 12s for the support widget and support dashboard.

6. Product Forms:
   - Fixed the `isActive` toggle form field name (from `active` to `isActive`) and persisted the boolean value successfully in the database.

7. Legacy Imports:
   - Refactored all 19 occurrences of legacy imports `@/lib/numbers` and `@/utils/persian` to use the unified `@/lib/persian` utility.

8. Global Wishlist State:
   - Implemented `WishlistContext.tsx` to load the user's wishlist in a single session request.
   - Refactored `WishlistButton.tsx` to read/write state dynamically and optimistically from the context, eliminating massive per-button HTTP requests.

9. Database Analytics Aggregation:
   - Replaced memory-heavy event array retrievals (fetching up to 30,000 events in memory) with Prisma database aggregates (`groupBy` and `count`), optimizing query speed and minimizing memory usage.

10. Interactive Users Search:
    - Added a client-side user filter component (`UsersContentClient.tsx`) to search the users list by name and phone number dynamically.

11. Caching & Font Fallback:
    - Refactored cache life configurations (`cacheLife`) using explicit TTL configuration objects (`stale`, `revalidate`, `expire`).
    - Registered and embedded the Yekan brand font inside the global error boundary.

Verification:
- TypeScript typecheck (`npx tsc --noEmit`) completed with 0 errors.
- ESLint (`npm run lint`) completed with 0 errors.
